# Church Booking Backend

Run instructions (PowerShell):

```powershell
cd c:\Users\Jeff\church-booking-backend
npm install
npm run dev
```

Notes:
- `nodemon.json` is present in the project root and configured to run `src/app.js`.
- Keep running commands from the project root to avoid duplicate `src/src` paths.
- `package.json` is at the project root and uses `type: module` to support ES6 imports.
